{
  "name": "squad-up-backend",
  "description": "Backend system for SquadUp.",
  "version": "0.1.0",
  "private": true,
  "author": "Jacob Mendelowitz",
  "engines": {
    "node": ">=4.3.2"
  },
  "scripts": {
    "deploy": "gcloud app deploy",
    "start": "node app.js",
    "lint": "samples lint",
    "pretest": "npm run lint",
    "system-test": "samples test app",
    "test": "npm run system-test",
    "e2e-test": "samples test deploy"
  },
  "dependencies": {
    "@google-cloud/datastore": "^1.1.0",
    "body-parser": "^1.18.2",
    "express": "4.15.4"
  },
  "devDependencies": {
    "@google-cloud/nodejs-repo-tools": "1.4.17"
  },
  "cloud-repo-tools": {
    "requiresProjectId": true,
    "requiresKeyFile": true,
    "test": {
      "app": {},
      "build": {}
    }
  }
}
